----------Sub Queries----------


1. Find details of all employees who have a higher salary than the average salary.
			select * from hr.employees where salary > 
			(
					select avg(salary) from hr.employees
			);
			
2. Find the count of employees who have a lower salary than the average salary.
			select count(EMPLOYEE_ID) from hr.employees where salary < 
			(	
					select avg(salary) from hr.employees
			);

----------Joins----------

1. Show Employee details along with the Department Name

			select EMPLOYEE_ID, FIRST_NAME, SALARY, e.DEPARTMENT_ID, DEPARTMENT_NAME
			from hr.employees e JOIN hr.departments d ON e.DEPARTMENT_ID = d.DEPARTMENT_ID;
